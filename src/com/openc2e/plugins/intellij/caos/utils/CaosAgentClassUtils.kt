package com.openc2e.plugins.intellij.caos.utils

data class AgentClass(val family:Int, val genus:Int, val species:Int) {
    fun like(otherClass:AgentClass) : Boolean {
        if (family == 0) {
            if (genus == 0 || otherC)
        }
        if (family != otherClass.family && otherClass.family != 0 && family != 0)
    }
}

object CaosAgentClassUtils {

    fun toClass

}